It's time for your BuJo ritual.

Invoke `/workbench-bujo:bujo` — the orchestrator will determine which tiers apply today (yearly if Jan 1, monthly if the 1st, weekly if Sunday, always daily) and chain them in strict order before the daily runs.

If no user is present when this fires, pause at the first interactive prompt and wait. Never fabricate responses. Never auto-complete a ritual. The session will remain paused until Mike picks it up.
